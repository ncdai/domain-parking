<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%= domain %></title>

  <% if (isForSale) { %>
    <meta name="description" content="The domain name <%= domain %> is for sale." />
  <% } else { %>
    <meta name="description" content="Our new website is launching soon." />
  <% } %>

  <meta name="theme-color" content="#040D17" />

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="tailwind.css?t=1709376299">

  <style>
    body {
      transition: opacity 0.3s ease-in-out;
    }

    .text-full-width {
      display: inline-block;
      transform-origin: 0 0;
      transform: translate3d(0, 0, 0);
    }
  </style>
</head>

<body style="opacity: 0;">
  <div class="fixed bg-primary-900 bg-grid-primary-800/[0.5] w-full h-full top-0 left-0 flex items-center justify-center p-4">
    <div class="absolute top-0 left-0 bottom-0 right-0 [mask-image:radial-gradient(ellipse_at_center,transparent_10%,black)] bg-primary-900 pointer-events-none"></div>

    <div class="relative z-10 w-screen space-y-2">
      <div class="transition-colors duration-300 hover:text-blue-600">
        <h1 class="font-medium text-center text-full-width">
          <%= domain %>
        </h1>
      </div>

      <div class="sticky bottom-4">
        <% if (isForSale) { %>
          <div class="flex flex-wrap gap-4 justify-center items-center flex-col md:flex-row">
            <span class="flex items-center px-4 py-2 rounded-full bg-primary-800 font-medium">Domain name is for sale</span>
            <a href="mailto:hello@quaric.com?subject=<%= domain %>" target="_blank" rel="noreferrer" class="flex rounded-full px-4 py-2 font-medium transition-colors bg-white text-primary-900 hover:bg-primary-100 select-none">Make an offer</a>
          </div>
        <% } else { %>
          <p class="sm:text-2xl text-primary-300 text-center">Our new website is <strong class="text-white font-normal">launching soon</strong>.</p>
        <% } %>
      </div>
    </div>
  </div>

  <script src="text-full-width.js"></script>
</body>

</html>